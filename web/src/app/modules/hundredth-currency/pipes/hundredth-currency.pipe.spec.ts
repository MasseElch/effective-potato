import { HundredthCurrencyPipe } from './hundredth-currency.pipe';

describe('HundredthCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new HundredthCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
