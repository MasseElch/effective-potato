include:
    - parameters.yaml

App\Entity\CategoryBudgetAtMonth:

    categoryBudgetAtMonth (template):
        year: <(Cake\Chronos\Date::now()->year)>
        month: <(Cake\Chronos\Date::now()->month)>
        budget: <(new Money\Money($account_balance, new Money\Currency($account_currency)))>
        category: '@defaultCategory_<current()>'

    # add two entries for each budget (jcl, mur, shared)
    categoryBudgetAtMonth_{jcl, mur, shared} (extends categoryBudgetAtMonth):
